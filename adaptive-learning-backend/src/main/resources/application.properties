# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/adaptive_learning_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Deepak3319,.,.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Enable security debug logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.web=DEBUG

# JWT Refresh Token Expiration time (24 hours in milliseconds)
jwt.refresh.token.expiration.ms=86400000
